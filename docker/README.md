# TON Lite Client Docket

Build: `docker-compose build`

Run lite-client: `docker-compose run --rm lite test-lite-client -D /data -C /data/config.json`

Run interative bash inside container: `docker-compose run --rm bash`
